({
    onLookupIdChanged: function(cmp, evt, h) {
        h.lookupIdChanged(cmp, evt);
    },

    onContactIdChanged: function(cmp, evt, h) {
        h.contactIdChanged(cmp, evt);
    },

    onInit: function(cmp, evt, h) {
        h.init(cmp, evt);
    }
})
